{
  "name": "cursor-agent",
  "description": "Cursor tools collection — OpenClaw plugin + opencli CDP plugin",
  "plugins": {
    "cursor-cdp": {
      "path": "cdp-plugin",
      "version": "0.1.0",
      "description": "Control Cursor IDE via Chrome DevTools Protocol (CDP) — send prompts, read chat, switch models, browse history, take screenshots, manage projects"
    }
  }
}
